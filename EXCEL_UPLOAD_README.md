# Excel 일괄 업무 등록 시스템

## 개요
캠페인 상세 페이지에서 Excel 파일을 업로드하여 여러 업무를 한 번에 등록할 수 있는 시스템입니다.

## 주요 기능

### 1. Excel 템플릿 다운로드
- 모달 상단에서 템플릿 다운로드 버튼 클릭
- 템플릿에는 **7개 필수 컬럼**이 포함되어 있음
- 예시 데이터 3개 행 포함 (블로그, SNS, 비디오)
- **사용 가이드 시트** 포함 (두 번째 시트)

### 2. 필수 입력 컬럼 (7개)
1. **업무 타입** (workType): 상품 관리에 등록된 업무 타입
2. **제품명** (productName): 상품 관리에 등록된 제품명
3. **수량** (quantity): 1 이상의 숫자
4. **시작일** (startDate): YYYY-MM-DD 형식
5. **마감일** (dueDate): YYYY-MM-DD 형식
6. **매출** (budget): 0 이상의 숫자
7. **재무 상태** (financialStatus): 미발행, 발행완료, 지급완료

### 3. 자동 설정 항목 (사용자 입력 불필요)
- **원가** (cost): 업무타입 + 제품명으로 자동 매칭
- **업무 내용** (title): 제품명으로 자동 설정
- **승인 상태** (topicStatus): 기본값 "미정"
- **세부사항 검토** (outline): 빈 값
- **세부사항 승인 상태** (outlineStatus): 기본값 "미정"
- **반려 사유** (rejectionReason): 빈 값
- **결과물 링크** (publishedUrl): 빈 값

### 4. 파일 업로드
- 드래그 앤 드롭 또는 파일 선택으로 업로드
- 지원 형식: .xlsx, .xls

### 5. 데이터 검증
- 업로드 즉시 자동 유효성 검증
- 필수 필드 확인 (7개 항목)
- 데이터 타입 검증 (숫자, 날짜, 선택 가능한 값 등)
- **업무 타입 동적 검증** (API에서 가져온 목록과 비교)
- 오류가 있는 행 강조 표시
- 오류 상세 내역 표시

### 6. 데이터 미리보기
- 업로드한 모든 데이터를 테이블로 표시
- 행 번호와 함께 모든 컬럼 표시
- 오류가 있는 행은 빨간색 배경으로 강조

### 7. 일괄 등록
- 유효성 검증 통과 시 등록 버튼 활성화
- **제품 목록 자동 조회 및 원가 매칭**
- 진행률 표시 (현재/전체)
- 개별 행 단위로 등록 처리
- 성공/실패 통계 표시

### 8. 결과 확인
- 성공한 업무 목록
- 실패한 업무 목록 (오류 메시지 포함)
- 자동 새로고침 (모두 성공 시)

## 사용 방법

1. 캠페인 상세 페이지에서 "Excel 일괄등록" 버튼 클릭
2. 모달에서 "템플릿 다운로드" 버튼 클릭
3. 다운로드한 Excel 파일의 "사용 가이드" 시트를 확인
4. "캠페인 업무" 시트에서 데이터 입력
   - 첫 번째 행(헤더)은 수정하지 마세요
   - 두 번째 행부터 데이터 입력
   - **7개 필수 컬럼**만 입력하면 됩니다
   - 예시 데이터(블로그, SNS, 비디오) 참고
5. 작성한 Excel 파일을 모달에 업로드
   - 드래그 앤 드롭 또는 "파일 선택" 버튼 사용
6. 데이터 미리보기 및 유효성 검증 확인
   - 오류가 있다면 Excel 파일을 수정하고 다시 업로드
7. "N개 업무 등록" 버튼 클릭
8. 업로드 진행 상황 확인
9. 결과 확인 및 모달 닫기

## 검증 규칙

### 필수 필드
- 7개 필수 컬럼은 모두 입력해야 합니다

### 데이터 타입
- **수량**: 1 이상의 숫자만 입력 가능
- **매출**: 0 이상의 숫자만 입력 가능
- **시작일, 마감일**: YYYY-MM-DD 형식 (예: 2025-01-15)

### 선택 가능한 값
- **업무 타입**: 상품 관리 메뉴에 등록된 업무 타입만 사용 가능 (동적으로 검증)
- **재무 상태**: 미발행, 발행완료, 지급완료 중 하나

### 자동 매칭
- **원가**: 업무 타입과 제품명이 상품 관리에 등록되어 있으면 자동으로 매칭됩니다
- **업무 내용**: 제품명이 자동으로 설정됩니다

## 구현 파일

### 신규 파일
1. **src/utils/excelUtils.js**
   - Excel 템플릿 생성
   - Excel 파일 파싱
   - 데이터 유효성 검증
   - API 형식 변환

2. **src/components/modals/ExcelUploadModal.jsx**
   - Excel 업로드 UI
   - 파일 처리 로직
   - 데이터 미리보기
   - 일괄 등록 처리

### 수정된 파일
1. **src/components/campaigns/CampaignDetail.jsx**
   - Excel 업로드 버튼 추가
   - 모달 상태 관리
   - 성공 핸들러 추가

2. **package.json**
   - xlsx 라이브러리 추가

## 기술 스택
- **xlsx**: Excel 파일 파싱 및 생성
- **React**: UI 컴포넌트
- **Lucide React**: 아이콘
- **Tailwind CSS**: 스타일링

## 주의사항
1. 템플릿의 헤더(첫 번째 행)를 수정하지 마세요
2. 모든 필수 컬럼을 빠짐없이 입력하세요
3. 데이터 형식을 정확히 맞춰주세요 (특히 날짜와 선택 값)
4. 대용량 파일(수백 행 이상)의 경우 업로드에 시간이 걸릴 수 있습니다
5. 일부 행이 실패해도 성공한 행은 정상적으로 등록됩니다

## 향후 개선 가능 사항
- [ ] 파일 크기 제한 추가
- [ ] 업로드 취소 기능
- [ ] 데이터 편집 기능 (업로드 전)
- [ ] 부분 재시도 기능 (실패한 행만)
- [ ] 트랜잭션 처리 (전체 성공/실패)
- [ ] 비동기 처리 (백그라운드 작업)
