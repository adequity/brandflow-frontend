# Excel 일괄 업무 등록 시스템

## 개요
캠페인 상세 페이지에서 Excel 파일을 업로드하여 여러 업무를 한 번에 등록할 수 있는 시스템입니다.

## 주요 기능

### 1. Excel 템플릿 다운로드
- 모달 상단에서 템플릿 다운로드 버튼 클릭
- 템플릿에는 14개 필수 컬럼이 포함되어 있음
- 예시 데이터 행 포함 (참고용)

### 2. 필수 컬럼 (14개)
1. **업무 타입** (workType): 블로그, 인스타그램, 유튜브, 기타
2. **제품명** (productName): 제품 이름
3. **수량** (quantity): 숫자
4. **원가** (cost): 숫자
5. **업무 내용** (title): 업무 제목/내용
6. **시작일** (startDate): YYYY-MM-DD 형식
7. **마감일** (dueDate): YYYY-MM-DD 형식
8. **승인 상태** (topicStatus): 미정, 대기, 승인, 반려
9. **세부사항 검토** (outline): 텍스트
10. **세부사항 승인 상태** (outlineStatus): 미정, 대기, 승인, 반려
11. **반려 사유** (rejectionReason): 텍스트 (비어있을 수 있음)
12. **매출** (budget): 숫자
13. **재무 상태** (financialStatus): 미발행, 발행완료, 지급완료
14. **결과물 링크** (publishedUrl): URL (비어있을 수 있음)

### 3. 파일 업로드
- 드래그 앤 드롭 또는 파일 선택으로 업로드
- 지원 형식: .xlsx, .xls

### 4. 데이터 검증
- 업로드 즉시 자동 유효성 검증
- 필수 필드 확인
- 데이터 타입 검증 (숫자, 날짜, 선택 가능한 값 등)
- 오류가 있는 행 강조 표시
- 오류 상세 내역 표시

### 5. 데이터 미리보기
- 업로드한 모든 데이터를 테이블로 표시
- 행 번호와 함께 모든 컬럼 표시
- 오류가 있는 행은 빨간색 배경으로 강조

### 6. 일괄 등록
- 유효성 검증 통과 시 등록 버튼 활성화
- 진행률 표시 (현재/전체)
- 개별 행 단위로 등록 처리
- 성공/실패 통계 표시

### 7. 결과 확인
- 성공한 업무 목록
- 실패한 업무 목록 (오류 메시지 포함)
- 자동 새로고침 (모두 성공 시)

## 사용 방법

1. 캠페인 상세 페이지에서 "Excel 일괄등록" 버튼 클릭
2. 모달에서 "템플릿 다운로드" 버튼 클릭
3. 다운로드한 Excel 파일을 열고 데이터 입력
   - 첫 번째 행(헤더)은 수정하지 마세요
   - 두 번째 행부터 데이터 입력
   - 모든 14개 컬럼은 필수입니다
4. 작성한 Excel 파일을 모달에 업로드
   - 드래그 앤 드롭 또는 "파일 선택" 버튼 사용
5. 데이터 미리보기 및 유효성 검증 확인
   - 오류가 있다면 Excel 파일을 수정하고 다시 업로드
6. "N개 업무 등록" 버튼 클릭
7. 업로드 진행 상황 확인
8. 결과 확인 및 모달 닫기

## 검증 규칙

### 필수 필드
- 모든 14개 컬럼은 필수이며 비워둘 수 없습니다

### 데이터 타입
- **수량, 원가, 매출**: 숫자만 입력 가능
- **시작일, 마감일**: YYYY-MM-DD 형식 (예: 2025-01-15)

### 선택 가능한 값
- **업무 타입**: 블로그, 인스타그램, 유튜브, 기타 중 하나
- **승인 상태**: 미정, 대기, 승인, 반려 중 하나
- **세부사항 승인 상태**: 미정, 대기, 승인, 반려 중 하나
- **재무 상태**: 미발행, 발행완료, 지급완료 중 하나

## 구현 파일

### 신규 파일
1. **src/utils/excelUtils.js**
   - Excel 템플릿 생성
   - Excel 파일 파싱
   - 데이터 유효성 검증
   - API 형식 변환

2. **src/components/modals/ExcelUploadModal.jsx**
   - Excel 업로드 UI
   - 파일 처리 로직
   - 데이터 미리보기
   - 일괄 등록 처리

### 수정된 파일
1. **src/components/campaigns/CampaignDetail.jsx**
   - Excel 업로드 버튼 추가
   - 모달 상태 관리
   - 성공 핸들러 추가

2. **package.json**
   - xlsx 라이브러리 추가

## 기술 스택
- **xlsx**: Excel 파일 파싱 및 생성
- **React**: UI 컴포넌트
- **Lucide React**: 아이콘
- **Tailwind CSS**: 스타일링

## 주의사항
1. 템플릿의 헤더(첫 번째 행)를 수정하지 마세요
2. 모든 필수 컬럼을 빠짐없이 입력하세요
3. 데이터 형식을 정확히 맞춰주세요 (특히 날짜와 선택 값)
4. 대용량 파일(수백 행 이상)의 경우 업로드에 시간이 걸릴 수 있습니다
5. 일부 행이 실패해도 성공한 행은 정상적으로 등록됩니다

## 향후 개선 가능 사항
- [ ] 파일 크기 제한 추가
- [ ] 업로드 취소 기능
- [ ] 데이터 편집 기능 (업로드 전)
- [ ] 부분 재시도 기능 (실패한 행만)
- [ ] 트랜잭션 처리 (전체 성공/실패)
- [ ] 비동기 처리 (백그라운드 작업)
